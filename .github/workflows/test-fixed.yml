name: Test Fixed Action

on:
  workflow_dispatch:
  push:

jobs:
  test-clean:
    name: Clean notebook (should pass)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: kunalagra/nbstripout@refactor/gh-actions-refactor
        with:
          paths: "clean.ipynb"

  test-dirty:
    name: Dirty notebook (should fail)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: kunalagra/nbstripout@refactor/gh-actions-refactor
        with:
          paths: "dirty.ipynb"

  test-special-chars:
    name: Special char filename (fixed handles without bash errors)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: kunalagra/nbstripout@refactor/gh-actions-refactor
        with:
          paths: "My Analysis (draft).ipynb"

  test-all:
    name: All notebooks via glob (fixed handles without bash errors)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: kunalagra/nbstripout@refactor/gh-actions-refactor
        with:
          paths: "**/*.ipynb"
