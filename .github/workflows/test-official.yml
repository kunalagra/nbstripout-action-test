name: Test Official Action

on:
  workflow_dispatch:
  push:

jobs:
  test-clean:
    name: Clean notebook (should pass)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: kynan/nbstripout@main
        with:
          paths: "clean.ipynb"

  test-dirty:
    name: Dirty notebook (should fail)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: kynan/nbstripout@main
        with:
          paths: "dirty.ipynb"

  test-special-chars:
    name: Special char filename (official fails with bash syntax error)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: kynan/nbstripout@main
        with:
          paths: "My Analysis (draft).ipynb"

  test-all:
    name: All notebooks via glob (official fails with bash syntax error)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: kynan/nbstripout@main
        with:
          paths: "**/*.ipynb"
